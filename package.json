{
  "name": "de-raaskalderij",
  "version": "1.0.0",
  "private": true,
  "description": "The static editorial home of De Raaskalderij.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "npm run build && npx serve dist"
  }
}
